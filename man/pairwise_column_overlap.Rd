% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crunch_helpers.R
\name{pairwise_column_overlap}
\alias{pairwise_column_overlap}
\title{Pairwise overlap of distinct values between columns of two tables}
\usage{
pairwise_column_overlap(
  left,
  right,
  left_cols = names(left),
  right_cols = names(right),
  max_distinct = 5000L,
  seed = NULL
)
}
\arguments{
\item{left, right}{Data frames.}

\item{left_cols}{Column names in `left` to evaluate.}

\item{right_cols}{Column names in `right` to evaluate.}

\item{max_distinct}{Maximum distinct values retained per column before
comparison.}

\item{seed}{Optional RNG seed so sampling is reproducible when sets are large.}
}
\value{
A `data.frame` sorted by descending `jaccard`.
}
\description{
Builds a Jaccard-style similarity score for every pair of columns listed from
`left` and `right`. Use it to rank plausible identifier or code columns when
joining unfamiliar extracts.
}
\section{Limitations}{

\itemize{
\item High overlap can reflect chance, shared status codes, or linkage that
is not appropriate for merges.
\item Values are compared as character strings after `as.character()`.
\item When distinct counts exceed `max_distinct`, values are subsampled
independently per column.
}
}
