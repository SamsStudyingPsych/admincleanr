% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crunch_helpers.R
\name{coerce_best_datetime}
\alias{coerce_best_datetime}
\title{Coerce to Date/POSIXct by trying several parse orders}
\usage{
coerce_best_datetime(
  x,
  orders = .admincleanr_default_parse_orders(),
  tz = "UTC",
  sample_size = 8000L,
  result = c("auto", "Date", "POSIXct"),
  excel_serial = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{x}{Vector of input values (`character`, `Date`, `POSIXt`, or numeric for
Excel serial dates).}

\item{orders}{Character vector of `parse_date_time` order strings.}

\item{tz}{Time zone for parsed datetimes.}

\item{sample_size}{Maximum number of non-empty values to use when scoring
orders.}

\item{result}{Return `Date`, `POSIXct`, or `auto` (date if all times are
midnight).}

\item{excel_serial}{If `TRUE`, attempt Excel serial conversion for numeric
`x`.}

\item{quiet}{If `FALSE`, prints the chosen order and NA rate on non-empty
strings.}
}
\value{
`Date` or `POSIXct` vector aligned with `x`.
}
\description{
Samples non-missing values, evaluates candidate `orders` with
\code{lubridate::parse_date_time()}, and keeps the order that yields the fewest
failed parses on that sample. The winning order is then applied to the full
vector.
}
\details{
Numeric vectors in a range typical of Excel serial days are coerced via an
origin of `1899-12-30` when `excel_serial = TRUE`.
}
\section{Intended use}{

Ad hoc exploration and one-off extracts. Once formats are understood, encode
them explicitly with a fixed parser in production scripts.
}

\section{Limitations}{

\itemize{
\item Ambiguous strings can parse cleanly while still reflecting the wrong
calendar intent.
\item When there are more than `sample_size` non-empty rows, the best order
is selected from a random sample.
\item Ties on the loss score default to the first matching order in
`orders`.
\item Time zones follow `lubridate`; compare `tz` with source-system
policy.
}
}
