# admincleanr_crunch (consolidated)

**These functions now ship with the main `admincleanr` package.** After `library(admincleanr)`, call them directly:

- `coerce_best_datetime()`
- `coerce_best_datetime_cols()`
- `pairwise_column_overlap()`

This companion package is kept for backward compatibility but no longer needs to be installed separately.
