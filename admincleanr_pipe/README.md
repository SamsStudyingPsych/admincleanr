# admincleanr_pipe (consolidated)

**Pipeline scaffold functions now ship with the main `admincleanr` package.** After `library(admincleanr)`, call them directly—no separate install needed.

This companion package is kept for backward compatibility but no longer needs to be installed separately.
